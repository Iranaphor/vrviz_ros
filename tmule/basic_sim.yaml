---
session: vrviz_basic_sim
init_cmd: |
  #exec /bin/bash
  source ~/bashrc
  vrviz_server

windows:

- name: gazebo
  panes:
  - roslaunch vrviz_ros turtlebot_sim.launch
  - gzclient
  - rviz
  check: "rostopic list | grep -q odom"

- name: mqtt
  panes:
  - roslaunch vrviz_ros mqtt.launch

#- name: vr_config
#  panes:
#  - rostopic echo /vrviz/config